# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: Studio*264 Creative Agency
- **분석 날짜**: 2025-11-25
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: 모던 네온 미니멀리즘

## 🎯 CSS 변수 정의

```css
:root {
  /* Primary Colors - 네온 옐로우 시스템 */
  --color-primary: #CFFF00;
  --color-primary-light: #E4FF1A;
  --color-primary-dark: #BAFF00;
  --color-primary-glow: rgba(207, 255, 0, 0.3);

  /* Secondary Colors - 다크 시스템 */
  --color-secondary: #000000;
  --color-secondary-light: #1A1A1A;
  --color-accent: #E4FF1A;

  /* Background Colors */
  --color-bg-primary: #000000;
  --color-bg-secondary: #F8F8F8;
  --color-bg-tertiary: #FFFFFF;
  --color-bg-card: #FAFAFA;

  /* Text Colors */
  --color-text-primary: #000000;
  --color-text-secondary: #FFFFFF;
  --color-text-muted: #666666;
  --color-text-light: #999999;

  /* Border & Outline Colors */
  --color-border-light: #E0E0E0;
  --color-border-medium: #CCCCCC;
  --color-border-dark: #333333;

  /* Interactive Colors */
  --color-button-primary: #CFFF00;
  --color-button-primary-text: #000000;
  --color-button-hover: #E4FF1A;
  --color-button-active: #BAFF00;
  --color-link: #CFFF00;
  --color-link-hover: #E4FF1A;

  /* Shadow & Effects */
  --shadow-neon: 0 0 20px rgba(207, 255, 0, 0.5);
  --shadow-soft: 0 2px 8px rgba(0, 0, 0, 0.1);
  --shadow-strong: 0 4px 16px rgba(0, 0, 0, 0.2);
}
```

## 🎨 컬러 사용 가이드

### Primary Color (#CFFF00 - 네온 라임 옐로우)
**사용 목적**: 브랜드 아이덴티티, 주요 CTA, 시선 유도
- ✅ **권장 사용**
  - 메인 CTA 버튼 (CONTACT, 구매하기 등)
  - 로고 강조 요소
  - 중요 헤드라인 하이라이트
  - 호버 효과 및 포커스 상태
  - 그라데이션 배경의 핵심 색상

- ❌ **피해야 할 사용**
  - 본문 텍스트 (가독성 저하)
  - 넓은 배경 영역 (시각적 피로)
  - 섬세한 아이콘 (너무 강렬함)

```css
/* 사용 예시 */
.cta-button {
  background-color: var(--color-primary);
  color: var(--color-button-primary-text);
  box-shadow: var(--shadow-neon);
}

.cta-button:hover {
  background-color: var(--color-button-hover);
  transform: translateY(-2px);
}
```

### Secondary Color (#000000 - 순수 검은색)
**사용 목적**: 배경, 텍스트, 강력한 대비 효과
- ✅ **권장 사용**
  - 메인 배경색
  - 제목 및 본문 텍스트
  - 로고 텍스트
  - 네비게이션 바 배경
  - 섹션 구분선

```css
/* 사용 예시 */
body {
  background-color: var(--color-bg-primary);
  color: var(--color-text-secondary);
}

.hero-title {
  color: var(--color-text-primary);
  background: var(--color-bg-secondary);
}
```

### Accent Color (#E4FF1A - 밝은 네온 옐로우)
**사용 목적**: 호버 상태, 강조점, 인터랙션 피드백
- ✅ **권장 사용**
  - 버튼 호버 효과
  - 링크 활성화 상태
  - 알림 배지
  - 하이라이트 효과
  - 그라데이션 밝은 영역

### Background Colors
```css
/* 레이어링 시스템 */
.layer-1 { background: var(--color-bg-primary); }    /* #000000 - 최하단 */
.layer-2 { background: var(--color-bg-secondary); }  /* #F8F8F8 - 콘텐츠 */
.layer-3 { background: var(--color-bg-tertiary); }   /* #FFFFFF - 카드 */
.layer-4 { background: var(--color-bg-card); }       /* #FAFAFA - 네스팅 */
```

### Text Colors
```css
/* 텍스트 계층 구조 */
.text-hero { color: var(--color-text-primary); font-weight: 700; }
.text-title { color: var(--color-text-primary); font-weight: 600; }
.text-body { color: var(--color-text-secondary); font-weight: 400; }
.text-caption { color: var(--color-text-muted); font-weight: 400; }
.text-disabled { color: var(--color-text-light); font-weight: 300; }
```

## 🌓 다크모드 변수 (선택사항)

```css
[data-theme="dark"] {
  /* 다크모드에서는 기존 컬러가 이미 최적화되어 있음 */
  --color-bg-primary: #000000;
  --color-bg-secondary: #1A1A1A;
  --color-bg-tertiary: #2A2A2A;
  --color-text-primary: #FFFFFF;
  --color-text-secondary: #E0E0E0;
  --color-text-muted: #999999;
}

[data-theme="light"] {
  /* 라이트모드 대체 팔레트 */
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F8F8F8;
  --color-bg-tertiary: #F0F0F0;
  --color-text-primary: #000000;
  --color-text-secondary: #1A1A1A;
  --color-text-muted: #666666;
  --color-primary: #BAFF00; /* 라이트모드에서는 약간 어둡게 */
}
```

## 📱 반응형 고려사항

### 모바일 (< 768px)
- 네온 옐로우 글로우 효과 강도 낮추기 (성능)
- 버튼 크기 충분히 확보 (최소 44x44px)
- 텍스트 대비 높이기

```css
@media (max-width: 768px) {
  :root {
    --shadow-neon: 0 0 10px rgba(207, 255, 0, 0.3); /* 모바일에서 가벼운 효과 */
  }

  .cta-button {
    min-height: 48px;
    font-size: 16px; /* iOS 자동 줌 방지 */
  }
}
```

### 태블릿 (768px ~ 1024px)
- 적당한 여백과 간격 유지
- 터치 친화적인 인터랙션 영역

### 데스크톱 (> 1024px)
- 풀 글로우 효과 적용
- 부드러운 애니메이션
- 호버 인터랙션 강화

```css
@media (min-width: 1025px) {
  .cta-button:hover {
    box-shadow: var(--shadow-neon);
    transition: all 0.3s ease;
  }
}
```

## 🎭 그라데이션 시스템

```css
/* 메인 히어로 그라데이션 */
.gradient-hero {
  background: radial-gradient(
    circle at 50% 50%,
    var(--color-primary-light) 0%,
    var(--color-primary) 30%,
    rgba(207, 255, 0, 0.5) 50%,
    transparent 70%
  );
  filter: blur(60px);
}

/* 버튼 그라데이션 */
.gradient-button {
  background: linear-gradient(
    135deg,
    var(--color-primary) 0%,
    var(--color-primary-light) 100%
  );
}

/* 텍스트 그라데이션 */
.gradient-text {
  background: linear-gradient(
    90deg,
    var(--color-primary) 0%,
    var(--color-accent) 100%
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
```

## ♿ 접근성 가이드

### 색상 대비 비율
- **네온 옐로우 (#CFFF00) vs 검은색 (#000000)**: 19.34:1 ✅ (AAA 등급)
- **흰색 (#FFFFFF) vs 검은색 (#000000)**: 21:1 ✅ (AAA 등급)
- **회색 텍스트 (#666666) vs 흰색 (#FFFFFF)**: 5.74:1 ✅ (AA 등급)

### 권장사항
```css
/* 포커스 표시 */
*:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* 스크린 리더 전용 텍스트 */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  border: 0;
}
```

## 🎪 실전 컴포넌트 예시

### 네비게이션
```css
.navbar {
  background-color: var(--color-bg-primary);
  border-bottom: 1px solid var(--color-border-dark);
}

.nav-link {
  color: var(--color-text-secondary);
  transition: color 0.2s ease;
}

.nav-link:hover {
  color: var(--color-primary);
}
```

### 카드
```css
.card {
  background: var(--color-bg-tertiary);
  border-radius: 16px;
  box-shadow: var(--shadow-soft);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-strong);
}
```

### CTA 버튼
```css
.btn-primary {
  background-color: var(--color-button-primary);
  color: var(--color-button-primary-text);
  padding: 14px 32px;
  border-radius: 50px;
  font-weight: 600;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
}

.btn-primary:hover {
  background-color: var(--color-button-hover);
  box-shadow: var(--shadow-neon);
  transform: translateY(-2px);
}

.btn-primary:active {
  background-color: var(--color-button-active);
  transform: translateY(0);
}
```

## 📊 컬러 사용 비율 권장

| 색상 | 비율 | 용도 |
|------|------|------|
| 검은색 (#000000) | 40% | 배경, 텍스트 |
| 네온 옐로우 (#CFFF00) | 10% | CTA, 강조 |
| 흰색/그레이 (#FFFFFF, #F8F8F8) | 45% | 콘텐츠 영역 |
| 기타 컬러 | 5% | 장식, 디테일 |

## 💡 디자인 팁

1. **60-30-10 법칙 적용**
   - 60%: 검은색 배경
   - 30%: 흰색/밝은 회색 콘텐츠
   - 10%: 네온 옐로우 포인트

2. **네온 효과 남용 금지**
   - 글로우 효과는 2-3개 핵심 요소에만 적용
   - 너무 많으면 시각적 피로 유발

3. **충분한 여백 확보**
   - 강렬한 색상 조합이므로 여백으로 균형 맞추기
   - 최소 20px 이상의 패딩 권장

4. **그라데이션 블러 활용**
   - 네온 컬러에 blur(60px~80px) 적용
   - 부드러운 분위기 연출

## 🔗 추가 리소스

- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)
- [Coolors Palette Generator](https://coolors.co/)
- [Adobe Color Accessibility Tools](https://color.adobe.com/create/color-accessibility)

---

**마지막 업데이트**: 2025-11-25
**버전**: 1.0.0
**작성자**: AI Assistant
