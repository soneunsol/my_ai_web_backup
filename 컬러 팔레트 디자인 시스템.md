# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: Talentadore (https://talentadore.com)
- **분석 날짜**: 2025-11-18
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 스타일**: 모던 다크모드, 그라디언트 강조

---

## 🎯 CSS 변수 정의

```css
:root {
  /* ==================== Primary Colors ==================== */
  --color-primary: #A855F7;              /* 메인 퍼플 - 브랜드 색상 */
  --color-primary-light: #C084FC;        /* 밝은 퍼플 - 호버 효과 */
  --color-primary-dark: #9333EA;         /* 진한 퍼플 - 액티브 상태 */

  /* ==================== Secondary Colors ==================== */
  --color-secondary: #FF6B9D;            /* 코랄 핑크 - 보조 색상 */
  --color-secondary-light: #FF8FB8;      /* 밝은 핑크 */
  --color-secondary-dark: #E94D7F;       /* 진한 핑크 */

  /* ==================== Accent Colors ==================== */
  --color-accent: #FF8E72;               /* 오렌지-살몬 - 강조 색상 */
  --color-accent-light: #FFB094;         /* 밝은 살몬 */
  --color-accent-dark: #FF6B4D;          /* 진한 살몬 */

  /* ==================== Background Colors ==================== */
  --color-bg-primary: #0B0B1E;           /* 메인 배경 - 다크 네이비 */
  --color-bg-secondary: #151529;         /* 서브 배경 - 밝은 네이비 */
  --color-bg-tertiary: #1A1A2E;          /* 카드/섹션 배경 */
  --color-bg-elevated: #1F1F3A;          /* 떠있는 요소 배경 */

  /* ==================== Text Colors ==================== */
  --color-text-primary: #FFFFFF;         /* 주요 텍스트 - 순백 */
  --color-text-secondary: #E5E5E5;       /* 본문 텍스트 - 약간 어두운 흰색 */
  --color-text-muted: #9CA3AF;           /* 보조 텍스트 - 연한 회색 */
  --color-text-disabled: #6B7280;        /* 비활성 텍스트 */

  /* ==================== Border Colors ==================== */
  --color-border-subtle: #2D2D44;        /* 미세한 테두리 */
  --color-border-default: #3F3F5C;       /* 기본 테두리 */
  --color-border-strong: #4B5563;        /* 강조 테두리 */

  /* ==================== Interactive Colors ==================== */
  --color-button-primary: #A855F7;       /* 기본 버튼 */
  --color-button-hover: #9333EA;         /* 버튼 호버 */
  --color-button-active: #7E22CE;        /* 버튼 클릭 */
  --color-link: #FF6B9D;                 /* 링크 색상 */
  --color-link-hover: #A855F7;           /* 링크 호버 */

  /* ==================== Gradient Colors ==================== */
  --gradient-primary: linear-gradient(135deg, #FF8E72, #FF6B9D, #A855F7);
  --gradient-secondary: linear-gradient(135deg, #A855F7, #9333EA);
  --gradient-accent: linear-gradient(90deg, #FF8E72, #FF6B9D);
  --gradient-subtle: linear-gradient(180deg, rgba(168, 85, 247, 0.1), transparent);

  /* ==================== Shadow & Glow ==================== */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.5);
  --shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.6);

  --glow-primary: 0 0 20px rgba(168, 85, 247, 0.3);
  --glow-secondary: 0 0 20px rgba(255, 107, 157, 0.3);
  --glow-accent: 0 0 20px rgba(255, 142, 114, 0.3);

  /* ==================== Status Colors ==================== */
  --color-success: #10B981;              /* 성공 - 그린 */
  --color-warning: #F59E0B;              /* 경고 - 오렌지 */
  --color-error: #EF4444;                /* 에러 - 레드 */
  --color-info: #3B82F6;                 /* 정보 - 블루 */
}
```

---

## 🎨 컬러 사용 가이드

### 1️⃣ Primary Colors (메인 색상)
**사용 목적**: 브랜드 아이덴티티를 나타내는 핵심 색상
- **주요 버튼**: CTA(Call-to-Action) 버튼, 제출 버튼
- **로고 및 아이콘**: 브랜드를 대표하는 시각 요소
- **강조 요소**: 중요한 정보나 섹션 제목
- **진행 표시**: 프로그레스 바, 로딩 인디케이터

```css
/* 사용 예시 */
.btn-primary {
  background: var(--color-primary);
  color: var(--color-text-primary);
}

.btn-primary:hover {
  background: var(--color-primary-dark);
  box-shadow: var(--glow-primary);
}
```

### 2️⃣ Secondary Colors (보조 색상)
**사용 목적**: Primary를 보완하고 시각적 계층 구조 형성
- **보조 버튼**: 덜 중요한 액션 버튼
- **링크**: 텍스트 링크 및 네비게이션
- **배지**: 알림, 태그, 라벨
- **아이콘 강조**: 특정 상태나 카테고리 표시

```css
/* 사용 예시 */
.link {
  color: var(--color-link);
  transition: color 0.3s ease;
}

.link:hover {
  color: var(--color-link-hover);
}
```

### 3️⃣ Accent Colors (강조 색상)
**사용 목적**: 시선을 끌어야 하는 특별한 요소
- **프로모션 배너**: 이벤트, 할인 정보
- **알림 배지**: 새로운 메시지, 업데이트
- **하이라이트**: 검색 결과, 선택된 항목
- **데코레이션**: 시각적 흥미를 더하는 요소

```css
/* 사용 예시 */
.badge-new {
  background: var(--color-accent);
  color: var(--color-text-primary);
  border-radius: 12px;
  padding: 4px 12px;
}
```

### 4️⃣ Background Colors (배경 색상)
**사용 목적**: 콘텐츠의 계층과 깊이 표현
- **Primary**: 페이지 전체 배경
- **Secondary**: 섹션 구분, 컨테이너
- **Tertiary**: 카드, 모달
- **Elevated**: 드롭다운, 툴팁

```css
/* 사용 예시 */
body {
  background: var(--color-bg-primary);
}

.card {
  background: var(--color-bg-tertiary);
  border: 1px solid var(--color-border-subtle);
}

.modal {
  background: var(--color-bg-elevated);
  box-shadow: var(--shadow-xl);
}
```

### 5️⃣ Gradient Colors (그라디언트)
**사용 목적**: 시각적 흥미와 프리미엄 느낌 제공
- **히어로 섹션**: 첫 화면의 임팩트
- **주요 CTA**: 전환을 유도하는 버튼
- **제목 강조**: 섹션 타이틀 언더라인
- **배경 효과**: 미묘한 그라디언트 오버레이

```css
/* 사용 예시 */
.hero-title {
  background: var(--gradient-primary);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.cta-button {
  background: var(--gradient-primary);
  position: relative;
  overflow: hidden;
}

.cta-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.5s;
}

.cta-button:hover::before {
  left: 100%;
}
```

---

## 📱 반응형 고려사항

### 🌙 다크모드 (기본)
현재 팔레트는 다크모드를 기본으로 설계되었습니다.

```css
/* 다크모드 (기본) */
:root {
  /* 위의 모든 변수 사용 */
}
```

### ☀️ 라이트모드 (옵션)
필요시 라이트모드 대응을 위한 색상 조정:

```css
/* 라이트모드 대체 색상 */
[data-theme="light"] {
  /* Background - 밝게 반전 */
  --color-bg-primary: #F9FAFB;
  --color-bg-secondary: #F3F4F6;
  --color-bg-tertiary: #FFFFFF;
  --color-bg-elevated: #FFFFFF;

  /* Text - 어둡게 반전 */
  --color-text-primary: #111827;
  --color-text-secondary: #374151;
  --color-text-muted: #6B7280;
  --color-text-disabled: #9CA3AF;

  /* Border - 밝은 회색 */
  --color-border-subtle: #E5E7EB;
  --color-border-default: #D1D5DB;
  --color-border-strong: #9CA3AF;

  /* Primary Colors - 약간 어둡게 조정 (가독성) */
  --color-primary: #9333EA;
  --color-primary-light: #A855F7;
  --color-primary-dark: #7E22CE;

  /* Shadow - 부드럽게 */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
  --shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.1);
}
```

### 📐 브레이크포인트별 색상 적용

```css
/* 모바일 (< 768px) */
@media (max-width: 767px) {
  :root {
    /* 모바일에서는 그라디언트 단순화 (성능) */
    --gradient-primary: var(--color-primary);

    /* 그림자 감소 (성능) */
    --shadow-lg: var(--shadow-md);
    --shadow-xl: var(--shadow-lg);
  }
}

/* 태블릿 (768px ~ 1023px) */
@media (min-width: 768px) and (max-width: 1023px) {
  /* 기본 설정 유지 */
}

/* 데스크탑 (≥ 1024px) */
@media (min-width: 1024px) {
  :root {
    /* 큰 화면에서 글로우 효과 강화 */
    --glow-primary: 0 0 30px rgba(168, 85, 247, 0.4);
    --glow-secondary: 0 0 30px rgba(255, 107, 157, 0.4);
    --glow-accent: 0 0 30px rgba(255, 142, 114, 0.4);
  }
}
```

### 🎭 애니메이션 선호도 대응

```css
/* 애니메이션 줄이기 선호 사용자 */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }

  /* 그라디언트도 단순화 */
  :root {
    --gradient-primary: var(--color-primary);
    --gradient-secondary: var(--color-primary-dark);
  }
}
```

### 🌈 고대비 모드 지원

```css
@media (prefers-contrast: high) {
  :root {
    /* 텍스트 대비 강화 */
    --color-text-primary: #FFFFFF;
    --color-text-secondary: #F3F4F6;

    /* 테두리 명확히 */
    --color-border-default: #6B7280;
    --color-border-strong: #9CA3AF;

    /* 버튼 배경 진하게 */
    --color-button-primary: #7E22CE;
  }
}
```

---

## 🛠️ 실전 활용 팁

### ✨ 컬러 조합 추천

#### 1. 히어로 섹션
```css
.hero {
  background: var(--color-bg-primary);
  color: var(--color-text-primary);
}

.hero-title {
  background: var(--gradient-primary);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.hero-button {
  background: var(--gradient-primary);
  color: var(--color-text-primary);
  box-shadow: var(--glow-primary);
}
```

#### 2. 카드 컴포넌트
```css
.card {
  background: var(--color-bg-tertiary);
  border: 1px solid var(--color-border-subtle);
  color: var(--color-text-secondary);
  transition: all 0.3s ease;
}

.card:hover {
  border-color: var(--color-primary);
  box-shadow: var(--shadow-lg), var(--glow-primary);
  transform: translateY(-4px);
}

.card-title {
  color: var(--color-text-primary);
}

.card-tag {
  background: var(--color-accent);
  color: var(--color-text-primary);
}
```

#### 3. 내비게이션
```css
.nav {
  background: var(--color-bg-secondary);
  border-bottom: 1px solid var(--color-border-subtle);
}

.nav-link {
  color: var(--color-text-muted);
  transition: color 0.3s ease;
}

.nav-link:hover {
  color: var(--color-primary);
}

.nav-link.active {
  color: var(--color-text-primary);
  border-bottom: 2px solid var(--color-primary);
}
```

#### 4. 폼 요소
```css
.input {
  background: var(--color-bg-secondary);
  border: 1px solid var(--color-border-default);
  color: var(--color-text-primary);
}

.input:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
  outline: none;
}

.input::placeholder {
  color: var(--color-text-muted);
}
```

---

## 📊 접근성 체크리스트

- [ ] **대비율 검사**: 모든 텍스트가 WCAG AA 기준 (4.5:1) 충족
- [ ] **색맹 대응**: 색상만으로 정보 전달하지 않기
- [ ] **포커스 표시**: 키보드 내비게이션시 명확한 포커스 링
- [ ] **상태 표시**: 호버, 액티브, 디스에이블 상태 명확히 구분
- [ ] **다크모드 대비**: 라이트모드 전환시에도 가독성 유지

---

## 🎯 마무리 체크포인트

### ✅ 컬러 팔레트 구현 전 확인사항
1. 모든 CSS 변수가 정의되었는가?
2. 그라디언트 효과가 과하지 않은가?
3. 텍스트 가독성이 충분한가?
4. 모바일에서도 색상이 잘 보이는가?
5. 브랜드 아이덴티티를 잘 표현하는가?

### 💡 유지보수 가이드
- 새로운 색상 추가시 명명 규칙 준수
- 색상 변경시 관련 그라디언트도 함께 업데이트
- 정기적으로 접근성 검사 실시
- 사용하지 않는 색상 변수는 제거

---

**📌 참고**: 이 디자인 시스템은 Talentadore의 시각적 스타일에서 영감을 받아 제작되었으며, 포트폴리오 프로젝트에 맞게 커스터마이징할 수 있습니다.
